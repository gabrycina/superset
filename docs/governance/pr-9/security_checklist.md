# Security Checklist (OWASP-aligned) — PR #9: Experimental SAML SSO login provider

> STATUS: **REQUIRES HUMAN APPROVAL** — auto-generated by the change-governance
> reviewer; a security engineer must review and sign off before merge.

Scope: `superset/security/demo_sso_provider.py::register_sso_provider(app,
metadata_url)`. The function today only appends `metadata_url` to
`app.config["SSO_PROVIDERS"]` and returns a dict. Items are marked for the code
**as written** and, where relevant, as **gating requirements** for the real SAML
implementation this stub anticipates.

## Authentication & session (OWASP A07 / ASVS V2, V3)
- [ ] **Assertion signature verification** — REQUIRED before wiring. Stub does no
  SAML validation; a real provider must verify assertions are signed by the
  metadata cert. **NOT MET (stub).**
- [ ] **Audience / Destination / Recipient checks** — REQUIRED before wiring.
- [ ] **Replay & time-window enforcement** (`NotBefore`/`NotOnOrAfter`,
  `InResponseTo`, one-time `AssertionID`) — REQUIRED before wiring.
- [ ] **Session fixation** — regenerate the session on SSO login — REQUIRED before
  wiring.

## Authorization (OWASP A01 / SECURITY.md matrix)
- [ ] **IdP-attribute → role mapping is explicit + allow-listed** — no implicit
  Admin from IdP claims. Stub has none. **Must be defined before enabling.**
- [ ] **Provider registration is an operator/Admin-only action** — the eventual
  caller of `register_sso_provider` must not be reachable by Public/Gamma. Name
  the exact `SECURITY.md` matrix row + attacker principal in the threat model.

## Input validation (OWASP A03)
- [ ] **`metadata_url` validated** — https-only, host allow-list, reject
  malformed URLs. **NOT MET:** value is stored verbatim.
- [ ] **SSRF protection** — REQUIRED before any fetch of `metadata_url`; block
  loopback/link-local/cloud-metadata (169.254.169.254) and private ranges.
  **NOT MET (no fetch yet, no guard).** Highest-priority gate.
- [ ] **XML parsing hardening** — when SAML XML is parsed, disable external
  entities / DTDs (XXE, billion-laughs). Use a vetted SAML lib (e.g.
  `python3-saml`) with `defusedxml`. **N/A today, REQUIRED before wiring.**

## Secrets management (OWASP A02 / A05)
- [ ] **SP private key / certificate stored in secret store**, not `app.config`
  literals, env dumps, or VCS. **N/A today, REQUIRED before wiring.**
- [ ] `metadata_url` is not secret-grade but must not be logged at info level or
  returned to unauthenticated callers.

## Logging & monitoring (OWASP A09)
- [ ] **Audit-log provider registration** (who/when/which URL) and every auth
  decision (success/failure, subject, IdP). **NOT MET:** no logging in the
  function.
- [ ] Ensure logs never contain raw SAML assertions, tokens, or keys.

## Output / data exposure (OWASP A04 design)
- [ ] The returned dict `{"provider","metadata_url"}` must not be echoed to
  unauthenticated HTTP responses (reveals internal IdP endpoints). Review the
  eventual caller. **Watch item.**

## Code quality / supply chain (OWASP A06 / A08)
- [ ] Type hints + docstring per repo standards (`app: Flask`, `metadata_url:
  str`, return type). **NOT MET:** untyped params.
- [ ] Any new SAML dependency pinned, ≥7 days old, from a trusted source; run
  dependency audit. **N/A today.**
- [ ] Feature flag, default **off**, for the provider. **NOT MET / TODO.**

## Tests
- [ ] Unit + integration tests for validation, signature, role mapping, SSRF
  rejection. Current PR ships none for the runtime path (a governance smoke test
  is added — see `tests.md`). **Author must add real tests before wiring.**

## Overall assessment
As an inert stub the immediate exploitable risk is low, but it ships an
authentication primitive with **no signature validation, no input/SSRF
validation, no audit logging, no role-mapping guardrails, and no type safety**.
It MUST NOT be wired into the security manager until the REQUIRED items above are
met and this checklist is signed off.

## Sign-off
- [ ] Security engineer reviewed — **REQUIRED**
- [ ] SECURITY.md matrix row + attacker principal named in threat model — **REQUIRED**
